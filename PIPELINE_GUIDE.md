# WorldLLM 시스템 파이프라인 가이드

> 검토용 문서입니다. 실제 기능을 변경하기 전에 이 구조와 데이터 흐름을 기준으로 영향 범위를 확인합니다.

## 1. 시스템 목적

WorldLLM은 TRPG 스타일의 세계관 데이터를 DB로 관리하고, LLM으로 세계관을 설계·상세화·시뮬레이션·수정하는 웹 시스템입니다.

핵심 원칙은 다음과 같습니다.

- 엔트리의 변경은 가능한 한 **제안 → 사용자 승인 → 저장** 순서를 따릅니다.
- LLM 연결 설정과 API 키는 사용자 계정별로 분리합니다.
- 임베딩은 선택 기능이며, 꺼져 있어도 키워드 기반 RAG와 일반 기능은 동작합니다.
- 로컬 LLM에서는 많은 항목을 한 번에 생성하지 않고, 설계 목록을 기준으로 작은 묶음으로 순차 호출합니다.

## 2. 구성 요소

```text
Browser
  └─ Flask UI / API (WorldLLM)
       ├─ SQLite DB
       │   ├─ users / user_llm_settings
       │   ├─ worlds / world_entries / entry_relationships
       │   └─ simulation_* / timelines / snapshots
       ├─ LLM endpoint (OpenAI 호환)
       │   ├─ Ollama / LM Studio / vLLM
       │   ├─ OpenAI / DeepSeek
       │   └─ OpenRouter (Claude 모델 포함)
       ├─ Optional embedding endpoint
       └─ NovelAI image endpoint
```

## 3. 권한 및 사용자 파이프라인

1. 사용자는 아이디·비밀번호·이름으로 가입 요청을 합니다.
2. 계정은 기본적으로 승인 대기 상태입니다.
3. `admin` 계정이 사용자 승인 화면에서 승인합니다.
4. 승인된 사용자만 로그인해 세계관 기능과 LLM 설정을 사용할 수 있습니다.
5. LLM API 키, 임베딩 API 키, NovelAI API 키, 모델명, 접점 주소는 사용자별 `user_llm_settings`에 저장됩니다.

현재 세계관 DB 자체는 사용자별로 분리되지 않고 공용입니다. 즉 **LLM 설정은 개인별**, 세계관 데이터는 현재 선택한 공용 세계관 단위입니다.

## 4. LLM 연결 설정 파이프라인

마스터 설정에서 아래를 저장합니다.

| 항목 | 용도 |
|---|---|
| LLM 제공자 프리셋 | 로컬, OpenAI, DeepSeek, OpenRouter, Copilot, 직접 입력 |
| LLM 주소 / API 키 | 대화·설계·시뮬레이션·AI 수정에 사용 |
| 시뮬레이션 모델 | 틱·요약·타임라인 생성 모델 |
| NAI 프롬프트 생성 모델 | NovelAI 태그 생성용 텍스트 모델 |
| NovelAI API 키 | 이미지 생성 요청에 사용 |
| 임베딩 주소 / API 키 / 모델 | 선택적 벡터 검색에 사용 |

`모델 불러오기`는 설정된 OpenAI 호환 endpoint의 `/v1/models`를 호출합니다. 목록을 지원하지 않는 서버는 모델명을 직접 입력합니다.

Claude 직접 API는 OpenAI 호환 형식이 아니므로, 현 구조에서는 **OpenRouter endpoint로 Claude 모델을 선택**하는 방식을 사용합니다.

## 5. 세계관 엔트리 데이터 구조

`WorldEntry`의 주요 데이터는 다음과 같습니다.

| 필드 | 역할 |
|---|---|
| title / category / content | 엔트리의 기본 정보 |
| references | 다른 엔트리 ID 참조, 관계도 연결에도 사용 |
| primary_year | 대표 연도/시대 표기 |
| year_notes | 연도별 특기사항 확장 필드 |
| keywords / auto_tags | 키워드 및 LLM 자동 태그 |
| embedding_json | 선택적 벡터 임베딩 |
| created_by / tick_created | 생성 주체 및 발생 틱 |
| parent_entry_id / version_note | 시뮬레이션 버전 체인 |

### TRPG 작성 형식

- **인물**: D&D 스타일 캐릭터 시트(종족·직업·레벨, 능력치, HP/AC, 배경, 성향, 기술, 장비, 목표, 관계, 약점)
- **장소**: 지형, 주요 구역, 주민/세력, 위험, 비밀, 모험 훅, 접근 경로
- **세력**: 목표, 조직, 자원, 지도자, 동맹/적대, 현재 계획, 약점
- **사건/연도**: 발생 연도, 원인, 전개, 결과, 관련 대상, 영향

연도가 설정된 엔트리는 `## 연도별 특기사항` 섹션을 통해 해당 시점의 변화·사건·관계를 우선 기록하도록 LLM에 지시합니다.

## 6. 세계관 설계 파이프라인

세계관 설계는 대량 생성 대신 3단계로 진행합니다.

```text
큰 맥락 + 보완 답변
  → 1. 설계 목록 생성
       (제목, 분류, 핵심 속성, 생성 목적만 생성)
  → 기존 DB 중복 검사
       ├─ 제목 중복 제거
       └─ 임베딩 활성 시 벡터 유사도 0.88 이상 제외
  → 사용자 선택
  → 2. 상세 생성 분할 호출
       예: 10개 / 5회 = 2개씩 5회 호출
  → 상세 미리보기
  → 3. 선택 항목 DB 저장
```

상세 생성 글자 수는 마스터 설정의 `LLM 생성 엔트리 최대 글자 수`를 따릅니다.

- `0`: 모델이 충분하다고 판단하는 범위까지 작성
- 숫자: 엔트리당 최대 글자 수

## 7. 시뮬레이션(틱) 파이프라인

```text
SimulationRun 시작
  → 해당 사용자 LLM 설정 선택
  → 활성 엔트리 / 타임라인 비트 / 최근 로그 수집
  → 필요 시 컨텍스트 요약
  → 임베딩 또는 키워드 RAG로 참조 엔트리 축소
  → LLM 1회 호출로 현재 틱 결과 생성
  → 이벤트 / 새 엔트리 / 버전 / 소멸 상태를 DB 반영
  → 타임라인 및 로그 기록
```

`틱당 새 엔트리 생성 수`는 마스터 설정에서 지정합니다.

- `1~20`: 한 틱에 만들 수 있는 새 엔트리의 최대 수
- `0`: 모델이 꼭 필요하다고 판단한 수만 생성

유저가 작성한 원본 엔트리는 시뮬레이션이 직접 수정하지 않습니다. 변경이 필요하면 LLM 파생 버전을 만들고 원본을 보존합니다.

## 8. 대화형 AI 수정 파이프라인

```text
엔트리 선택 → AI 수정
  → 사용자가 수정 요청 입력
  → LLM이 질문 또는 수정 제안 반환
  → 사용자가 추가 대화로 요청 보완 가능
  → 수정안 미리보기
  → "이 수정안으로 저장" 승인 시에만 DB 업데이트
```

수정 제안 과정은 DB를 변경하지 않습니다. 승인된 경우에만 제목·분류·내용·참조가 갱신됩니다.

## 9. 관계 파이프라인

엔트리 목록에서 체크박스를 선택합니다.

- Shift 선택: 첫 항목과 Shift 선택 항목 사이를 일괄 선택
- 관계 연결: 첫 선택 엔트리를 출발점으로, 나머지 선택 엔트리에 관계 유형과 설명을 추가

관계는 `entry_relationships`에 저장되고, 기존 `references`도 동기화되어 관계도 화면에 표시됩니다.

## 10. Markdown 및 임베딩

엔트리 `content`는 Markdown을 저장할 수 있으며, 상세 화면에서 정화된 HTML로 렌더링합니다. 허용되지 않은 HTML/스크립트는 제거합니다.

임베딩을 켜면 다음 시점에 사용됩니다.

- 엔트리 생성/수정 시 임베딩 저장
- 세계관 설계의 중복 후보 검사
- 시뮬레이션의 틱별 관련 컨텍스트 선택
- 의미 검색 API

임베딩 endpoint 오류 시에는 기존 키워드 기반 RAG로 계속 진행하도록 설계되어 있습니다.

## 11. Docker / Synology 운영

- 서비스 포트: `4318`
- DB 볼륨: `./data` → `/data`
- 이미지 볼륨: `./storage` → `/app/storage`
- 컨테이너 DB URL: `sqlite:////data/worldbuilding.db`
- SQLite 안정성을 위해 Gunicorn worker는 1개로 운영합니다.

외부 사용자는 WorldLLM만 접속하고, 컨테이너는 내부 LAN의 로컬 LLM endpoint로 요청합니다. 따라서 Ollama 포트를 외부 인터넷에 공개할 필요가 없습니다.

## 12. 배포 전 확인

```bash
cd /volume4/llm_lore_create/llm_test
git pull --ff-only
docker compose up -d --build
docker compose ps
docker compose logs --tail=100 worldllm
```

Git 및 배포 명령의 공통 규칙은 `MUST_READ_UPDATES.md`를 반드시 확인합니다.

## 13. 검토가 필요한 향후 과제

1. 세계관/엔트리 자체도 사용자별로 완전히 분리할지 여부
2. API 키의 DB 암호화(현재는 원문을 API 응답으로 노출하지 않지만 DB 레벨 암호화는 미구현)
3. Claude 직접 API 지원 여부(현재는 OpenRouter 경유 권장)
4. 관계 유형·설명을 관계도 화면의 간선 라벨로 표시
5. 연도별 특기사항을 별도 UI 편집기와 타임라인에 자동 동기화
6. 장시간 로컬 LLM 작업을 위한 진행률 표시/작업 큐

## 14. 메타데이터 스키마 파이프라인

세계관별 `메타데이터 설정` 메뉴에서 다음을 관리합니다.

- 닫힌 능력치 축과 티어 라벨, 캐릭터별 능력치 매트릭스
- 열린 스킬/특성 레지스트리와 생성 가이드라인
- 카테고리별 엔트리 템플릿(필수 여부·설명 포함)

캐릭터 상세 생성 시 활성 능력치 축, 해당 카테고리 템플릿, 스킬/특성 가이드, 기존 레지스트리 후보가 함께 프롬프트에 들어갑니다. LLM은 능력치에 숫자 티어만 반환하며, 스킬은 기존 ID 재사용 또는 신규 제안으로 분리합니다. 신규 제안은 사용자가 전체 상세안을 승인해 저장할 때 레지스트리에 편입됩니다.

## 15. 소설 모드 파이프라인

`소설` 메뉴에서 챕터 순서·제목·Markdown 본문·초안/공개 상태를 관리합니다.

```text
챕터 본문 저장
  → 엔트리 제목 + aliases 자동 키워드 매칭
  → novel_entity_mention 저장
  → 선택 시 엔트리 요약/관계도 링크 표시
  → 선택적으로 LLM 문맥 지칭 후보 요청
  → 사용자 승인 후 LLM mention 저장
```

이어쓰기 요청은 세계관/챕터 문체 설정, 등장 엔트리, 최근 챕터를 컨텍스트로 사용하며 결과는 제안으로만 표시됩니다. 사용자가 본문에 적용하고 다시 저장해야 DB에 반영됩니다.

엔트리 공개 범위는 `작가전용`, `챕터공개`, `완전공개`로 지정합니다. 소설 사이드 패널은 작가 모드를 끄면 현재 챕터 순서 기준으로 공개 가능한 내용만 표시합니다.

### 구현된 세부 동작

- 제목과 별칭은 trie 기반으로 긴 표현을 우선 인식하며 겹치는 mention을 제거합니다.
- 인식 구간은 카테고리 색상 밑줄 미리보기와 우측 목록에 함께 표시됩니다.
- 본문 선택 후 수동 태깅하거나, 대명사·문맥 지칭을 LLM에 제안받아 승인할 수 있습니다.
- 엔티티 상세 패널에는 능력치 티어 라벨과 연결된 스킬/특성이 함께 표시됩니다.
- 문체는 세계관 기본과 챕터 덮어쓰기를 별도로 저장하며, 빈 챕터 필드는 기본값을 상속합니다.
- 이어쓰기는 관련 과거 챕터 최대 3개를 키워드 RAG로 선택합니다. 임베딩이 꺼져 있어도 동작합니다.
- 신규 고유명사 후보는 자동 저장하지 않고 사용자가 검토·승인한 경우에만 엔트리가 됩니다.

## 16. 백업·복원과 삭제 무결성

JSON 백업 형식은 버전 4입니다. 세계관별 백업에는 능력치 스키마/값, 스킬 레지스트리/링크, 가이드라인, 템플릿, 챕터/mention, 문체, 공개 범위, 관계가 포함됩니다. 복원 시 기존 ID를 새 ID로 재매핑합니다.

엔트리 또는 세계관을 삭제하면 능력치 값, 스킬 링크, 공개 범위, 관계, 소설 mention을 먼저 정리합니다. API 키는 사용자별 비밀 설정이므로 백업 JSON에 원문을 넣지 않습니다. 기존 버전 백업에 신규 확장 정보가 없으면 기본 템플릿과 가이드라인을 자동 생성합니다.

## 17. 이번 버전 검증

외부 LLM 호출 없이 실행되는 회귀 테스트가 `tests/test_update_spec.py`에 있습니다.

```powershell
python -m unittest tests.test_update_spec -v
python -m py_compile app.py models.py llm_client.py simulation_engine.py
git diff --check
```
